# Server-only email delivery settings. Configure these in Vercel; never commit real values.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# Optional public HTTPS scheduling page. When unset, the request form remains the only booking path.
VITE_SCHEDULING_URL=
